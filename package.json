{
  "dependencies": {
    "@octokit/core": "^3.5.1",
    "fs-extra": "^9.0.1",
    "klaw-sync": "^6.0.0",
    "prettier": "^2.2.1",
    "yaml": "^1.10.2"
  },
  "scripts": {
    "build": "yarn clean && node scripts/gen-workflows.js && prettier --write .",
    "clean": "rm -f .github/workflows/*.yml"
  }
}
